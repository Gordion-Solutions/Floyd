[package]
name = "floyd"
version = "0.0.0"
edition = "2021"
description = "Rust MC/DC coverage analysis. Initial release in development."
license = "MIT OR Apache-2.0"
repository = "https://github.com/Gordion-Solutions/Floyd"
readme = "README.md"

[dependencies]
rustc-demangle = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
